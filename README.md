ScopeFlow - Hackathon MVP

Phase: Landing Page + Dashboard

Run:

1. npm install
2. npm run dev

This project is a Next.js app scaffold for the ScopeFlow hackathon MVP. It includes a demo local auth (localStorage), a Supabase client code-gig stub, and UI for a landing page and dashboard with empty states.
